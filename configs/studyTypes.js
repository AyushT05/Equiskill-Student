/**
 * Single source of truth for study type string values.
 * Import this anywhere you need to reference a study type —
 * API routes, Inngest functions, UI components.
 */
export const STUDY_TYPES = {
    FLASHCARD: "Flashcard",
    QUIZ: "Quiz",
    QNA: "Q&A",
    NOTES: "notes",
};
